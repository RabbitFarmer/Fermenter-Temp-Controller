#!/bin/bash
cd ~/FermenterApp
source .venv/bin/activate
python3 app.py &

# Wait for the app to start (adjust sleep as needed)
sleep 2

# Open the HTTP URL in your default browser
xdg-open http://127.0.0.1:5000   # For Linux
# open http://localhost:5000     # For macOS